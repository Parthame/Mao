//Code to get sum of a no.
#include<iostream>

using namespace std;

int main()
{
    int number = ;
    int sum = 0;
    while(number!=0) {
        int digit = number%10;
        number = number/10;
        sum += digit;
    }
    cout << sum;
    
}
